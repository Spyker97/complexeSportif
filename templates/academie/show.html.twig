{% extends 'base.html.twig' %}

{% block title %}Academie{% endblock %}

{% block body %}
    <h1>Academie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ academie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ academie.name }}</td>
            </tr>
            <tr>
                <th>Nbr_seances</th>
                <td>{{ academie.nbrSeances }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('academie_index') }}">back to list</a>

    <a href="{{ path('academie_edit', {'id': academie.id}) }}">edit</a>

    {{ include('academie/_delete_form.html.twig') }}
{% endblock %}
